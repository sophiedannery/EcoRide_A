{% extends 'base.html.twig' %}

{% block title %}Tableau de bord Admin
{% endblock %}

{% block body %}
	<h1>Crédits par jour, du
		{{ labels[0]|date('d/m/Y') }}
		au
		{{ labels[6]|date('d/m/Y') }}</h1>

	<p>Si le graphique ne s'affiche pas, actualisez-la page.</p>


	<a href="{{ path('app_admin_dashboard_credit', { week_start: preWeek}) }}" class="btn btn_outline-secondary" data-turbo="false">Semaine précédente</a>
	<a href="{{ path('app_admin_dashboard_credit', { week_start: nextWeek}) }}" class="btn btn-outline-secondary" data-turbo="false">Semaine suivante</a>

	<canvas id="creditsChart" width="800" height="400"></canvas>

	<a href="{{ path('app_admin')}}">retour</a>


	{% block javascripts %}
		{{ parent() }}


		<script>

			if (typeof window.creditsChartInstance === 'undefined') {
window.creditsChartInstance = null;
}

function initTrajetsChart() {
if (window.creditsChartInstance) {
window.creditsChartInstance.destroy();
}

const labelsRaw = {{ labels|json_encode|raw }};
const dataValuesRaw = {{ data|json_encode|raw }};

const labels = labelsRaw.map(dateStr => {
const d = new Date(dateStr);
const dd = String(d.getDate()).padStart(2, '0');
const mm = String(d.getMonth() + 1).padStart(2, '0');
return dd + '/' + mm;
});

const dataValues = dataValuesRaw;

const ctx = document.getElementById('creditsChart').getContext('2d');

window.trajetsChartInstance = new Chart(ctx, {
type: 'bar',
data: {
labels: labels,
datasets: [
{
label: 'Crédits',
data: dataValues,
backgroundColor: 'rgba(75, 192, 192, 0.5)',
borderColor: 'rgba(75, 192, 192, 1)',
borderWidth: 1
}
]
},
options: {
scales: {
x: {
title: {
display: true,
text: 'Jour'
}
},
y: {
beginAtZero: true,
max: 50,
ticks: {
stepSize: 1
},
title: {
display: true,
text: 'Nombre de crédits'
}
}
}
}
});

}

document.addEventListener('DOMContentLoaded', initTrajetsChart);
		</script>
	{% endblock %}


{% endblock %}
