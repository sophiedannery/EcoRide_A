{# templates/account/vehicule_new.html.twig #}
{% extends 'base.html.twig' %}

{% block title %}Ajouter un véhicule
{% endblock %}

{% block body %}
	<div class="container py-5">
		<h1 class="mb-5">Ajouter un véhicule</h1>

		{{ form_start(form) }}
		<div class="row">
			<div class="col-md-6 mb-3">
				{{ form_label(form.immatriculation) }}
				{{ form_widget(form.immatriculation, { attr: { class: 'form-control' } }) }}
				{{ form_errors(form.immatriculation) }}
			</div>
			<div class="col-md-6 mb-3">
				{{ form_label(form.datePremiereImmatriculation) }}
				{{ form_widget(form.datePremiereImmatriculation, { attr: { class: 'form-control' } }) }}
				{{ form_errors(form.datePremiereImmatriculation) }}
			</div>
		</div>
		<div class="row">
			<div class="col-md-6 mb-3">
				{{ form_label(form.marque) }}
				{{ form_widget(form.marque, { attr: { class: 'form-control' } }) }}
				{{ form_errors(form.marque) }}
			</div>
			<div class="col-md-6 mb-3">
				{{ form_label(form.modele) }}
				{{ form_widget(form.modele, { attr: { class: 'form-control' } }) }}
				{{ form_errors(form.modele) }}
			</div>
		</div>
		<div class="row">
			<div class="col-md-6 mb-3">
				{{ form_label(form.couleur) }}
				{{ form_widget(form.couleur, { attr: { class: 'form-control' } }) }}
				{{ form_errors(form.couleur) }}
			</div>
			<div class="col-md-6 mb-3">
				{{ form_label(form.placesDisponibles) }}
				{{ form_widget(form.placesDisponibles, { attr: { class: 'form-control', min: 1 } }) }}
				{{ form_errors(form.placesDisponibles) }}
			</div>
		</div>

		<div class="mb-4">
			{{ form_label(form.energie, 'Énergie', { label_attr: { class: 'form-label d-block' } }) }}
			<div class="d-flex flex-wrap gap-3">
				{% for choice in form.energie %}
					<div class="form-check">
						{{ form_widget(choice, {
          attr: {
            class: 'form-check-input',
            id: choice.vars.id
          }
        }) }}
						<label class="form-check-label" for="{{ choice.vars.id }}">
							{{ choice.vars.label|capitalize }}
						</label>
					</div>
				{% endfor %}
			</div>
			{{ form_errors(form.energie) }}
		</div>

		<button type="submit" class="btn btn-primary">Enregistrer mon véhicule</button>
		<a href="{{ path('app_account') }}" class="btn btn-secondary">Annuler</a>
		{{ form_end(form) }}
	</div>
{% endblock %}
